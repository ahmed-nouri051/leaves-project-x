namespace technicalTest.Models
{
    public enum LeaveType
    {
        Annual,
        Sick,
        Other
    }

    public enum RequestStatus
    {
        Pending,
        Approved,
        Rejected
    }
}
